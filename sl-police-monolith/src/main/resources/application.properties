server.port=8081

# DataSource
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5435/traffic_fine}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:traffic_fine}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:traffic_fine}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=slPoliceTr4ff1cF1n3S3cr3tK3y2024SecureKey!
jwt.expiration=86400000

# Logging
logging.level.com.slpolice=INFO
